<table class="table table-striped">
       <tr><%= attachment_image_tag user, :profile_image, format: 'jpeg', fallback: "no_image.jpg", size: "100x100" %></tr>


	 <tr>
	    <th>name</th>
			<th><%= user.name %></th>
		 </tr>

		 <tr>
			 <th>introduction</th>
			<th><%= user.introduction %></th>
		</tr>
</table>
<div class="row">
	<%= link_to '',edit_user_path(user.id), class: "col-lg-12 btn btn-default glyphicon glyphicon-wrench" %>
</div>